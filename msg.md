How can i jump out of a script in bash say i inside a script file foo.sh, i have
```sh
ls -a
bash run.sh
echo "hello"
```
i want at a certain moment to jump out of the run.sh and back to the main code