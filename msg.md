{"prompt": " I want you to act as a professional Android app Java software engineer and as Android Studio. Your role as a software engineer is to review my code and ensure it adheres to clean code guidelines, such as readability, simplicity, and maintainability. You should ensure proper use of variables, methods, and modularization while removing redundant code. As Android Studio, your role is to import necessary classes, fix typos, format the code, and ensure its validity. When I provide code, you must:\n', '\n', 'Provide only the corrected and formatted code, without explanations.\n', 'Summarize what you improved in the code in a concise manner after providing the corrected version.\n', If you're ready, here is my first code:\n", "user_message": "public class NumbersAdapter extends RecyclerView.Adapter<NumbersAdapter.NumberHolder> {\n    private final NumberObject[] mNumbers;\n    private final Context mContext;\n    public final NumFragment fragmentInstance;\n\n    public NumbersAdapter(Context ctx, NumberObject[] numbers,NumFragment f) {\n        this.mNumbers = numbers;\n        this.mContext = ctx;\n        fragmentInstance = f;\n    }\n\n\n    @Override\n    public NumberHolder onCreateViewHolder( ViewGroup parent, int viewType) {\n        View view = LayoutInflater.from(parent.getContext())\n                .inflate(R.layout.number_item_template, parent, false);\n        return new NumberHolder(view, mContext);\n    }\n\n    @Override\n    public void onBindViewHolder( NumberHolder holder, int position) {\n        holder.bind(mNumbers[position]);\n    }\n\n    @Override\n    public int getItemCount() {\n        return mNumbers.length;\n    }\n\n    static class NumberHolder extends RecyclerView.ViewHolder implements View.OnClickListener {\n        private final TextView mNumberType;\n        private final TextView mNumber;\n        private final Context context;\n        private NumberObject mNumberObject;\n        private static final int REQUEST_POSITION=1;\n\n        NumberHolder( View view, Context ctx) {\n            super(view);\n            context = ctx;\n            itemView.setOnClickListener(this);\n            mNumberType = itemView.findViewById(R.id.number_type);\n            mNumber = itemView.findViewById(R.id.number);\n        }\n\n        void bind(NumberObject number) {\n            mNumberObject = number;\n            mNumberType.setText(number.getNumberType());\n            mNumber.setText(String.valueOf(number.getNumber()));\n        }\n\n        @Override\n        public void onClick(View view) {\n          if (mNumberObject != null) {\n                Intent intent = NumberActivity.newIntent(context, mNumberObject.getId());\n                fragmentInstance.startActivityForResult(intent,REQUEST_POSITION);\n            }\n        }\n    }\n}"}