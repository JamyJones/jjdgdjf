{"prompt": " I want you to act as a professional Android app Java software engineer and as Android Studio. Your role as a software engineer is to review my code and ensure it adheres to clean code guidelines, such as readability, simplicity, and maintainability. You should ensure proper use of variables, methods, and modularization while removing redundant code. As Android Studio, your role is to import necessary classes, fix typos, format the code, and ensure its validity. When I provide code, you must:\n', '\n', 'Provide only the corrected and formatted code, without explanations.\n', 'Summarize what you improved in the code in a concise manner after providing the corrected version.\n', If you're ready, here is my first code:\n", "user_message": "public class NumberPagerActivity extends AppCompatActivity{\n  private static final String EXTRA_NUMBER_ID =\"com.jdevoc.List.number_id\";\n  private ViewPager mViewPager;\n  private NumberObject[] mNumberObjects;\n\n  public Intent newIntent(Context ctx,UUID numberId){\n    Intent intent = new Intent(ctx,NumberPagerActivity.class);\n    intent.putExtra(EXTRA_NUMBER_ID,numberId);\n    return intent;\n  }\n\n  @Override\n  public void onCreate(Bundle savedInstanceState){\n    super.onCreate(savedInstanceState);\n    setContentView(R.layout.activity_number_viewpager);\n\n    mViewPager = (ViewPager) findViewById(R.id.number_view_pager);\n    UUID numberId = (UUID) getIntent().getSerializableExtra(EXTRA_NUMBER_ID);\n    mNumberObjects = NumeberLab.getIntance(this).getNumbersArray();\n    FragmentManager fragmentManager = getSupportFragmentManager();\n    mViewPager.setAdapter(new FragmentStatePagerAdapter(fragmentManager){\n      @Override\n      public Fragment getItem(int position){\n        NumberObject num =mNumberObjects[position];\n        return MainFragment.newInStance(num.getId())\n      }\n      @Override\n      public int getCount(){\n        return mNumberObjects.length;\n      }\n    });\n  }\n}"}