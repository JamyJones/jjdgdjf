{"prompt": "None", "user_message": "Why does my app crash when i rotate the screen. Previously before checking whether fragment is null didn't exist, the app didn't crash but with every change in screen rotation led to creation of an additional fragment\npackage com.jdevoc.List;\n\nimport android.os.Bundle;\nimport android.app.Activity;\nimport android.app.Fragment;\nimport android.app.FragmentManager;\n\npublic class ListActivity extends Activity {\n\n    @Override\n    protected void onCreate(Bundle savedInstanceState) {\n        super.onCreate(savedInstanceState);\n        setContentView(R.layout.activity_list);\n\n        FragmentManager fm = getFragmentManager();\n\n        Fragment fragment = fm.findFragmentById(R.id.fragment_container);\n        if(fragment==null){\n          fragment = new DatesFragment();\n        }\n        fm.beginTransaction()\n          .add(R.id.fragment_container, fragment)\n          .commit();\n    }"}