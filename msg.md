{"prompt": "None", "user_message": "What is jwt\nAm in supabase database. Modify the following database policy so that it check with a column called \"UserId\" (it is also the primary key) that contain  id of authenticated users rather than email\ncreate policy \"Enable update for users based on email\"\non \"public\".\"User Profiles\"\nfor update using (\n  (select auth.jwt()) ->> 'email' = email\n) with check (\n  (select auth.jwt()) ->> 'email' = email\n);"}