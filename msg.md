{"prompt": " I want you to act as a professional Android app Java software engineer and as Android Studio. Your role as a software engineer is to review my code and ensure it adheres to clean code guidelines, such as readability, simplicity, and maintainability. You should ensure proper use of variables, methods, and modularization while removing redundant code. As Android Studio, your role is to import necessary classes, fix typos, format the code, and ensure its validity. When I provide code, you must:\n', '\n', 'Provide only the corrected and formatted code, without explanations.\n', 'Summarize what you improved in the code in a concise manner after providing the corrected version.\n', If you're ready, here is my first code:\n", "user_message": "public class NumbersAdapter extends RecyclerView.Adapter<NumbersAdapter.ViewHolder>{\n  private NumberObject[] mNumbers;\n  public static class NumberHolder extends RecyclerView.ViewHolder{\n    private TextView mNumberType;\n    private TextView mNumber;\n    public NumberHolder(View view){\n      super(view);\n      mNumberType = itemView.findViewById(R.id.number_type);\n      mNumber = itemView.findViewById(R.id.number);\n    }\n    public static void bind(NumberObject number){\n      mNumberType.setText(number.getNumberType());\n      mNumber.setText(number.getNumber());\n    }\n  }\n  public NumbersAdapter(NumberObject[] numbers){\n    mNumbers = numbers;\n  }\n  @Override\n  public NumberHolder onCreateViewHolder(ViewGroup viewGroup, int viewType){\n    View view = LayoutInflater.inflate(R.layout.number_item_template,viewGroup,false);\n    return new NumberHolder(view);\n  }\n  @Override\n  public void onBindViewHolder(ViewHolder holder, int position){\n    NumberObject number = mNumbers[position];\n    holder.bind(number);\n  }\n  @Override\n  public int getItemCount(){\n    return mNumbers.length;\n  }\n}"}