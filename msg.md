{"prompt": "None", "user_message": "Fix my post message below\n```\nSure, active recall works but what if you forget to recall something and it appears in the exam, what then. I think active recall should only be used to check on your retention, don't rely on it for everything\n```"}